<!DOCTYPE html>
<html lang="nl-BE">

<head>
  <meta charset="UTF-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Pokemon bekijken</title>

  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <%- include('partials/bootstrap') %>
  <link rel="stylesheet" href="../public/css/eigenPokemonBekijken.css">
  <link rel="stylesheet" href="css/style.css">
</head>

<body>
  <header>
    <%- include('partials/menu') %>
  </header>
  <main> 
    <h1><b>Welkom, <%= locals.currentUser.name %> </b> </h1>
    <p>HuidigePokemon: <%= locals.currentUser.huidigePokemon.naam %></p>
    <img id="pokemonImage" src="<%= locals.currentUser.huidigePokemon.sprite %>" alt="">
    <p>Naam: <%= locals.currentUser.huidigePokemon.naam %></p>
    <p>Attack stat: <%= locals.currentUser.huidigePokemon.stats%></p>
    
    <p> <b> Gevangen Pokemon:  </b>  </p>
    <section>
      
        <% locals.currentUser.catchPokemon.map((pokemon)=>{ %>
          <div>
            <img id="pokemonImage" src="<%= pokemon.sprite %>" alt="">
            <p>Naam: <%= pokemon.naam %></p>
            <p>Attack stat: <%= pokemon.stats%></p>
            
          </div>
        <% }) %>
       
    </section>
  </main>
  <%- include('partials/footer')  %> 